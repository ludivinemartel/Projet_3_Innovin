<form method="post" action="{{ path('app_cepage_delete', {'id': cepage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cepage.id) }}">
    <button class="btn">Delete</button>
</form>
