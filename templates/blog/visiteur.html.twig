{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} - Blog {% endblock title %}

{% block body %}
    {% if blogs is not empty %}
        <div class="blogDivContent">
            {% for blog in blogs %}
            <div class="blogDivCard">
                    <h2 class="blogTitle">{{ blog.title }}</h2>
                    <p class="blogDate">{{ blog.date ? blog.date|date('d-m-Y') : '' }}</p>
                    <p> <img class="blogImage" src="{{ vich_uploader_asset(blog) }}" alt="{{ blog.title }}"></p>
                    <p class="blogDescription">{{ blog.description }}</p>
                    <p class="blogShow"><a href="{{ path('app_blog_show_visiteur', {'slug': blog.slug}) }}">Lire plus</a></p>
            </div> 
            {% endfor %}
        </div>
    {% else %}
            <div class="carre">
                <h2 class="connexion" style="text-align: center;">Pas d'articles pour le moment</h2>  
            </div> 
    {% endif %}   
{% endblock %}
